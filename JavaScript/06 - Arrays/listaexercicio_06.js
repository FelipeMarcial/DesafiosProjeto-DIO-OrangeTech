// Crie um programa que dado um númeri imprima a sua tabuada.
const numeroTabuada = 5;
for (let i = 1; i <= 10; i++) {
    
    let resultadoTabuada;
    resultadoTabuada = (i * numeroTabuada);
    console.log(numeroTabuada + ' * ' + i + ' = ' + resultadoTabuada);
}